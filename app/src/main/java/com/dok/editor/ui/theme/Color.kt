package com.dok.editor.ui.theme

import androidx.compose.ui.graphics.Color

// DaVinci Resolve Inspired Dark Palette
val DokBackground = Color(0xFF141416) // Main window background - never pure black
val DokSurface = Color(0xFF1C1C1F)    // Panel background
val DokSurfaceElevated = Color(0xFF232326) // Card / inspector / tool panel background
val DokDivider = Color(0xFF2A2A2E)    // Thin low-contrast dividers
val DokAccent = Color(0xFF4A9EFF)     // DaVinci resolve blue accent
val DokAccentVariant = Color(0xFF2B78D6)
val DokSecondaryText = Color(0xFF8A8A90) // Muted secondary text
val DokPrimaryText = Color(0xFFE8E8EC)   // Crisp primary text
val DokTrackVideo = Color(0xFF2A4365)    // Slate blue for video tracks
val DokTrackAudio = Color(0xFF22543D)    // Deep teal/green for audio tracks
val DokTrackText = Color(0xFF553C9A)     // Purple for text/overlay tracks
val DokPlayhead = Color(0xFFFF5252)      // Red playhead line with diamond handle
val DokWarning = Color(0xFFED8936)
val DokSuccess = Color(0xFF48BB78)
